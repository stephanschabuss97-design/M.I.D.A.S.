'use strict';
/* PWA service worker stub (Phase 0).
 * No caching or offline behavior yet. This keeps runtime behavior unchanged.
 */

self.addEventListener('install', () => {
  // Intentionally empty until Phase 2 cache logic is added.
});

self.addEventListener('activate', () => {
  // Intentionally empty until cache cleanup and client claims are defined.
});
